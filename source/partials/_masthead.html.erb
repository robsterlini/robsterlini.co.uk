<header class="masthead" id="menu">
  <% image = yield_content(:hero) || current_page.data.hero %>
  <div class="masthead__hero" aria-hidden="true" style="background-image: url(/assets/images/hero/<%= image %>_full_blur.jpg);">
    <div class="masthead__hero__full" style="background-image: url(/assets/images/hero/<%= image %>_full.jpg);"></div>
  </div>
  <nav class="nav">
    <ul class="nav__list">
      <!--<% data.pages.masthead.each do |nav| %>
        <% if nav.masthead != false %>
          --><li class="nav__item">
            <%
              nav_label = nav.abbr ? "<abbr class='nav__label' title='#{nav.abbr.title}'>#{nav.abbr.label}</abbr>" : "<span class='nav__label'>#{nav.label}</span>"
              concat link_to nav_label, nav.slug == "" ? "/" : nav.slug, :class => "nav__link--#{nav.icon} ss-nav-#{nav.icon}", :title => nav.alt
            %>
          </li><!--
        <% end %>
      <% end %>-->
    </ul>
  </nav>
</header>
---
title: My journal
description: The occassional thought on things (probably not) worth writing about.
page_classes: page--journal page--full-hero
priority: 0.95
---

<div class="col">
  <h1 class="_h1"><%= current_page.data.title %></h1>
  <p class="_p1"><%= current_page.data.description %></p>
</div>
<div class="col col--break">
  <% page_articles.take(1).each_with_index do |article, i| %>
    <%
      if article.data.hero
        content_for(:hero, "blog/#{article.slug}")
      end %>
      <h2 class="_h3"><%= link_to article.title, article %></h2>
      <p class="_h4"><%= partial "partials/journal/posted", :locals => { :a => article } %> in <%= partial "partials/journal/category", :locals => { :a => article } %></p>
      <p class="_p"><em><%= article.data.description %></em></p>
      <p class="_p">
        <a href="<%= article.url %>" class="btn">Read the post</a>
        <a href="/archive" class="btn" title="See the rest of my blog entries">Browse the archive</a>
      </p>
  <% end %>
</div>
<div class="col col--break">
  <%= partial "partials/journal/categories" %>
</div>
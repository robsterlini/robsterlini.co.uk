---
title: Posts from the archive
description: Here’s a repository of all of my ramblings from over the years. Some of them are pretty old though, so 2014 me doesn’t necessarily agree with 2012 me!
nav: journal
---

<main id="content" class="wrapper">
  <%= partial "partials/hero", locals: {
    title: current_page.data.title,
    subtitle: current_page.data.description
  } %>
  <article class="group typeset">
    <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
      <h2 class="heading--gutter" id="<%= year %>"><%= year %></h2>
      <% articles.each_with_index do |article, i| %>
            <h3><%= link_to article.title, article %></h3>
            <p class="small-caps"><%= partial "partials/journal/posted", :locals => { :a => article } %><br /><%= partial "partials/journal/category", :locals => { :a => article } %></p>
      <% end %>
    <% end %>

    <%= partial "partials/journal/categories" %>
  </article>
</main>
